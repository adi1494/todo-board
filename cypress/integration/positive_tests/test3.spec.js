describe('test 3', function () {
  beforeEach(() => {
    cy.visit('http://localhost:3000/home');
  });
  it('deletes card', function () {

  })
})

const deleteCard = (_cardselector) => {
  // pending dev
}